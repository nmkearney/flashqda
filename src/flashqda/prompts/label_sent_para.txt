Context:

Consider the following {granularity}(s), which occur before the {granularity} to be analyzed:  
{context_window}

---

Task:

Read the following {granularity}:  
{item}

Determine whether any of the following labels apply:
{label_list}

---

Instructions:

1. Use only the label names listed above.
2. Do not invent new labels.
3. If no label applies, return ["none"] in the labels field.
4. Reasoning requested: {return_reasoning}
5. If reasoning requested is True, briefly explain why each returned label applies.
6. Return the answer using this JSON schema:
{json_schema}